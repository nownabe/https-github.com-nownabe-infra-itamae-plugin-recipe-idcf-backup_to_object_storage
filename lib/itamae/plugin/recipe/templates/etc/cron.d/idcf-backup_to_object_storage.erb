<%- node[:idcf][:backup_to_object_storage][:directories].each do |dir| %>
<%= dir[:schedule] %> root /bin/bash /opt/idcf/backup_to_object_storage.sh <%= dir[:path] %> <%= dir[:bucket] %> <%= dir[:expire] %><%= dir[:command] ? " '#{dir[:command]}'" : "" %> > /dev/null 2>&1
<%- end %>

